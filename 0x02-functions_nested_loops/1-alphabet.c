#include <stdio.h>
#include "main.h"

/**
 * main - a program that prints alphabet
 * in lower case
 *
 * Return: 0 (success)
 */
void print_alphabet(void);
{
	char alpha;

	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		_putchar('alpha');
	}
	_putchar('n\');
}
